-- =========================================================
-- CoffeeDesk — crear la base de datos (SOLO en XAMPP local)
-- En InfinityFree NO se ejecuta: la base ya se crea desde el panel.
-- =========================================================
CREATE DATABASE IF NOT EXISTS coffeedesk
    CHARACTER SET utf8mb4
    COLLATE utf8mb4_unicode_ci;
